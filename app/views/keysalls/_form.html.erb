<%= form_for(@keysall) do |f| %>
  <% if @keysall.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@keysall.errors.count, "error") %> prohibited this keysall from being saved:</h2>

      <ul>
      <% @keysall.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
